================================================================================
comments
================================================================================
# anything else here should be ignored

$foo = $bar # comment

$foo = '#'

# https:/foo.bar/
--------------------------------------------------------------------------------
(manifest
  (comment)
  (statement
    (variable
      (name))
    (operator)
    (variable
      (name)))
  (comment)
  (statement
    (variable
      (name))
    (operator)
    (string))
  (comment))
