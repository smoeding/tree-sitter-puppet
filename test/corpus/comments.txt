================================================================================
comments
================================================================================
# anything else here should be ignored

$foo = $bar # comment

$foo = '#'

# https:/foo.bar/
--------------------------------------------------------------------------------
(manifest
  (comment)
  (statement
    (variable
      (name))
    (variable
      (name)))
  (comment)
  (statement
    (variable
      (name))
    (single_quoted_string))
  (comment))
