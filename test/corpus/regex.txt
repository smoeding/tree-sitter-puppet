================================================================================
regex
================================================================================
/^[123]$/
--------------------------------------------------------------------------------
(manifest
  (statement
    (resource
      (regex))))

================================================================================
regex with escaped slash
================================================================================
/foo\/bar/
--------------------------------------------------------------------------------
(manifest
  (statement
    (resource
      (regex))))

================================================================================
regex with quote
================================================================================
/"/
--------------------------------------------------------------------------------
(manifest
  (statement
    (resource
      (regex))))

================================================================================
regex with capture
================================================================================
if $size =~ /^([0-9]+(\.[0-9]+)?)([KMGTPEkmgtpe])/ {
}
--------------------------------------------------------------------------------
(manifest
  (statement
    (resource
      (if_expression
        (variable
          (name))
        (regex)
        (body)))))
